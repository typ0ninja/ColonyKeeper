<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.team13.colonykeeper.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/yardActivity"
        android:name="com.team13.colonykeeper.YardActivity"
        android:label="YardActivity" />
    <activity
        android:id="@+id/hiveActivity"
        android:name="com.team13.colonykeeper.HiveActivity"
        android:label="HiveActivity" />
</navigation>